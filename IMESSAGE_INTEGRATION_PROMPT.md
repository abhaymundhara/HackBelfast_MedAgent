# MedAgent — iMessage Integration & Belfast Pivot Prompt

This prompt covers **what is left to do** to ship MedAgent for HackBelfast 2026. The Solana audit layer, three-tier policy, LangGraph supervisor, and HITL pause/resume are already built and **must not be re-implemented**. This prompt is execution-ready: do not re-plan, do not broaden scope, do not rebuild what already works.

If this prompt conflicts with `MEDAGENT_BUILD_SPEC.md`, `MEDAGENT_IMPLEMENTATION_PROMPT.md`, or `HEDERA_BRIEFING.md`, **follow this file**. Those docs are legacy (Hedera + web UI) and are being archived.

---

*Full spec saved for agent reference. See the user's prompt for complete content.*
