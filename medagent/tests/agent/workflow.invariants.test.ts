import { describe, it } from "vitest";

describe.skip("workflow invariants (moved)", () => {
  it("moved to lib/agent/__tests__/workflow.adversarial.test.ts", () => {
    // placeholder to keep Vitest from treating this file as an empty failed suite
  });
});
